import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    authWelcomeMenu: {
        width: '100%',
        flex: 1,
        rowGap: 16,
        justifyContent: 'flex-end',
        marginBottom: 22,
    },
})