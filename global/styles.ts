import { StyleSheet } from 'react-native';

export const global = StyleSheet.create({
    authMain: {
        flex: 1,
        alignItems: 'center',
    },
    authForm: {
        alignItems: 'center',
        width: '100%',
        rowGap: 9,
        marginTop: 31
    }
})